// 定义pageUrl映射

export const pageUrlMap = {
  user: '/users/list',
  role: '/role/list',
  good: '/goods/list',
  menu: '/menu/list'
}

// 定义删除操作的url
export const pageDeleteMap = {
  user: '/users',
  role: '/role'
}

export const addDataMap = {
  user: '/users',
  role: '/role'
}

export const editDataMap = {
  user: '/users',
  role: '/role'
}
